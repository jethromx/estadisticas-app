# URL base del backend (sin slash final)
# En desarrollo dejar vacío — Vite proxea /api → localhost:8080
# En producción apuntar al servicio de Render
VITE_API_BASE_URL=https://estadisticas-app-backend.onrender.com
